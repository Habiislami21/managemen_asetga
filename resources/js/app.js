import "./bootstrap";

import Alpine from "alpinejs";

window.Alpine = Alpine;

Alpine.start();

// Import Parallax
import Parallax from "parallax-js";

// Buat tersedia secara global (opsional)
window.Parallax = Parallax;
